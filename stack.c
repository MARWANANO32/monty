#include "monty.h"
/**
 * stack_s - print the top of stack
 * @top: the top of stack
 * @count: the line_number
 */
void stack_s(stack_t **top, unsigned int count)
{
	(void)top;
	(void)count;
	cv.flag = 0;
}
